import { HeroSlider, CategorySidebar, ProductGrid } from "../"

export const StorePage = () => {
  return (
    <div className="Store-page">
      STORE
      <HeroSlider />
      <CategorySidebar />
      <ProductGrid />
    </div>
  )
}
